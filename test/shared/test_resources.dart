import 'package:flutter/material.dart';

abstract class TestResources {
  static const initialPreviewSize = Size(200, 200);
  static const initialPreviewSizeJson = {
    "width": 200,
    "height": 200,
  };
}
